import { ReviewScreen } from "./ReviewScreen";

/*
later there will be a quiz screen etc
*/

const QuizScreen = ({ currentDeck }) => {
    return <ReviewScreen currentDeck={currentDeck} />;
};

export default QuizScreen;
